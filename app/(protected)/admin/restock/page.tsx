import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function RestockPage() {
  return (
    <PagePlaceholder
      title="Restock"
      description="Log a restock event against a machine and update stock levels."
      phase="Phase 3 (Restocking flow)"
    />
  );
}
