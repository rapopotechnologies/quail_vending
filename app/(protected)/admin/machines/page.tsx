import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function MachinesPage() {
  return (
    <PagePlaceholder
      title="Machines"
      description="Manage vending machines, slots, and per-machine stock levels."
      phase="Phase 2 (Core CRUD)"
    />
  );
}
